[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
fastapi = "*"
uvicorn = {extras = ["standard"], version = "*"}

[dev-packages]
black = "*"

[requires]
python_version = "3.9"

[scripts]
format = "black ."
dev = "uvicorn app.main:app --reload"